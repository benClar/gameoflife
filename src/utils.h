#ifndef UTILS_HEADER
#define UTILS_HEADER



#include <stdlib.h>
#include <stdio.h>

int getLength(char *arr);
int *charToInt(char c);
int power(int base, int exp);
int *strToInt(char *input);

#endif /* !UTILS_HEADER */

